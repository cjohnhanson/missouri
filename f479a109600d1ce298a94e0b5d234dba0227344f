Closing review round for the first publish to crates.io, PyPI, npm and
Homebrew. Five independent agents read f479a10, one for each declared
review. Each agent received its criteria, the diff and the repository
path, and nothing from the driver's thread.

Three verdicts are PASS. Two came back FAIL and are declined here, with
the reviewer's own words and the reason, as signoff-driver step 5
sanctions. Both declined findings are recorded in tisket issue ohs7 and
are fixable in a patch release. Neither reaches the irreversible class.

signoff[review-docs] PASS f479a10 swept 20 prose surfaces including error text, help text, and shipped comments; built f479a10 in scratch and ran every documented command, matching the tutorial's output byte for byte; minor: the reference says a transition's `stdout` is checked "in every mode" and `--check-only` does not check it
signoff[review-code] PASS f479a10 built the commit from `git archive` in scratch and ran `cargo test --all-features` green (154 unit plus 106 integration, 0 failed); read the backend, setup, recorder and assertion call sites beside the new docker guards and found no remaining host-command path that reaches DockerBackend's `unreachable!()` stubs; exercised the new refusal paths on the built binary; minor: the docker refusal is a hand-written runtime type check repeated at three call sites, and the `build` job's condition omits `create-release`.
signoff[review-deps] PASS f479a10 cargo audit over the 433-crate lockfile reports zero advisories and zero unmaintained warnings, the serde_yml/libyml swap to yaml_serde removes the two that existed, a licence sweep of every locked crate found no copyleft (MIT, Apache-2.0, ISC, BSD-3, Zlib, Unicode-3.0, CDLA-Permissive-2.0; the one LGPL string is r-efi's MIT-or-Apache disjunction), `cargo package --locked` builds and verifies the published tarball with docs/ present, the built binary runs `--version` and `docs`, and the declared 1.88 floor matches the highest rust-version in the locked set; minor: diataxis 0.1.1 is a niche choice against `include_str!` or rust-embed, its `cli` feature implies `store` and links gix, reqwest, rustls and aws-lc into the binary for +169 crates, third-party release actions are pinned to mutable tags, and binary channels carry no third-party licence notices.
signoff[review-tests] PASS f479a10 declined blocking finding, recorded in tisket ohs7; the reviewer's verdict was FAIL and its words were "neutralizing graph.states.is_empty() in no_entry_point (src/cli.rs:810) leaves all 154 lib tests and every integration suite green, so the empty-directory refusal the head commit adds is untested"; declined because the untested guard chooses between two refusal messages on a path that already refuses and already exits non-zero, so its loss costs a reader a better sentence rather than a wrong result; the same reviewer ran the full workspace suite green and confirmed the serve refusal is not vacuous by killing it
signoff[review-usability] PASS f479a10 declined blocking finding, recorded in tisket ohs7; the reviewer's verdict was FAIL and its words were "missouri list prints 0 path(s) and exits 0 in three states a caller cannot separate, no project at all, a graph with no entry point, and a valid suite, while run, validate, and docgen refuse the first two with exit 2 and a named cause"; declined because run, validate and docgen already refuse both broken states with a named cause, so no path reports a passing suite that does not exist; the same reviewer walked the getting-started tutorial end to end with output matching step for step and reached first success in four steps
