fresh-eyes: agent review of the gate workflow commit. Scope: the github block in gaff.yml, the generated gate.yml, and the CI branch in merge-gate.sh (MISSOURI_SANDBOX=preinstalled when CI is set). Two independent agent passes: the first predicted ubuntu failures from the executor and workflow sources; the second verified the fixes and approved. The four packages-declaring suites pass locally under the preinstalled backend. Mutation: none applied — the change is YAML plus a CI-only shell branch with no covering test; the first gate run on GitHub exercises that branch. Also in scope: the docker image pull step, which removes the silent skip of the docker-backend test on CI.
