# Machine-specific, rebuilt by uv
.venv/

# dbt logs contain timestamps
logs/

# dbt target/ is rebuilt non-deterministically on each run;
# assertions verify target/ exists and dbt debug passes
target/
.user.yml

# uv.lock drifts across environments and uv versions
uv.lock

# uv scaffolding drifts by uv version (src/ vs flat layout, the authors
# field from git config, the python-version-dependent requires-python).
# The assertions verify the real behavior, so uv's boilerplate is not
# compared byte-for-byte.
pyproject.toml
main.py
.python-version
.gitignore
src/
