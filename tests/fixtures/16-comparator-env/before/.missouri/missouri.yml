env:
  MAGIC_TOKEN: "comparator-sees-this"

transitions:
  - name: "write data"
    command: "echo hello > data.txt"
    target: "../after"
    comparators:
      files:
        - path: "data.txt"
          command: "check-env"
