transitions:
  - name: "meltano init"
    command: >-
      uv run meltano init --no-usage-stats meltano-project --force &&
      perl -pi -e 's/^project_id: .*/project_id: fixed/' meltano-project/meltano.yml
    target: "../meltano-initialized"
